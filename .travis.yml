language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-angular-directive' ]; then cd .. && eval "mv $currentfolder generator-angular-directive" && cd generator-angular-directive; fi

